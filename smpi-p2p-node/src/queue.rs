//! Shared memory queue for sending packets between processes on the same node.
pub(crate) struct Queue;

impl Queue {
    pub(crate) fn new(node_id: u64, local_id: u64, other_id: u64) -> Queue {
        Queue
    }
}
